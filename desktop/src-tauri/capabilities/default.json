{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window plus the updater (used by the Rust-side auto-update check on launch). The web UI itself makes no Tauri IPC calls.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
