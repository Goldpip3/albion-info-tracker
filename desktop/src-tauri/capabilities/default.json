{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The web UI makes no Tauri IPC calls — it is a plain web app loaded in the webview — so only the core defaults are granted.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
