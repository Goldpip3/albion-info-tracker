# The capture agent sidecar (agent-x86_64-pc-windows-msvc.exe) is dropped
# here at build time by the release workflow or the local build step. The
# binary itself is gitignored; this file keeps the directory in the tree.
